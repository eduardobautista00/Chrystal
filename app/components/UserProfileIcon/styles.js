import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    profileContainer: {
      borderRadius: 20,
      overflow: 'hidden',
      marginLeft: 10,
    },
    profileImage: {
      width: 40,
      height: 40,
      borderRadius: 20,
    },
  });

  export default styles;